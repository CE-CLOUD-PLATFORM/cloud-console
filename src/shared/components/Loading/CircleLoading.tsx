import { CircularProgress } from '@mui/material';

export default function CircleLoading() {
  return (
    <div className="flex flex-grow mt-5 items-center justify-center">
      <CircularProgress />
    </div>
  );
}
